<nav class="nav">
    <ul class="nav-list">
        {{#foreach navigation}}
        <li class="nav-item{{#if current}} nav-item-current{{/if}}"><a href="{{url}}">{{label}}</a></li>
        {{/foreach}}
    </ul>
</nav>
