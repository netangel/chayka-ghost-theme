{{!< default}}

{{#post}}
<article class="post">
    <header class="page-head wrap">
        <h1 class="post-title post-title-plain">{{title}}</h1>
    </header>
    <div class="wrap">
        <div class="prose">
            {{#if feature_image}}
            <figure class="kg-card kg-image-card">
                <img class="kg-image" src="{{img_url feature_image size="l"}}" alt="{{title}}">
            </figure>
            {{/if}}
            {{content}}
        </div>
    </div>
</article>
{{/post}}
